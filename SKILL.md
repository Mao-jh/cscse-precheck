---
name: cscse-precheck
description: 留服认证·拒批风险预审（按次收费，3.99 元/次）。当用户问「留服认证会被拒吗」「一年制硕士出境多少天才够」「中途回国影响认证吗」「我的学校还在认证名单吗」「证明信能不能先申请认证」「网课影响认证吗」「认证失败怎么办」时使用。输入留学画像（院校与国家、专业学制、网课情况、出入境记录、前置学历、学位状态），返回认证风险报告：风险等级与置信度、逐条雷点（对号口径条目编号+相似拒批/通过案例编号+整改建议）、高频拒因对照（院校名单、出入境比对、网课红线、证明信限制、前置学历核验）、认证流程要点（360元、官方无法加急、必须本人办理）、拒批后果与补救路径。
runtime: remote http
endpoint: https://airpriv.top/api/cscse-precheck
cost: 3.99 CNY / per_call
---

# 留服认证·拒批风险预审（集成支付版）

本 Skill 是支付宝 AI 收「自研商家」架构的**薄壳**：本地只做编排与转发，
支付与履约全部由后端完成，**不持有私钥、不下单、不碰资金**——
支付全程走支付宝官方 `alipay-bot` CLI 与官方收银台，信任面干净。

**本 Skill 已内嵌完整支付能力**：无需用户另行安装任何官方支付 Skill。

## 工作模型

先确定当前状态，再按状态行动：

| 当前状态 | 下一步 |
|---|---|
| 用户问留服认证风险、时长核验、院校名单（未发起请求） | 按「调用前必采字段」收集画像，然后按「调用流程」第 1 步发起请求 |
| 收到 HTTP 402 / Payment-Needed | 读取 `references/402-payment.md` 并执行其中的支付流程 |
| 已有待确认支付，用户继续操作或询问结果 | 读 `references/402-payment.md`，从「查询支付状态」继续 |
| 其他 Skill 已在处理支付 | 不要重复执行支付命令 |

## 调用前必采字段（触发付费前必须完成）

本工具的输入是**留学画像**而非文本——画像残缺会直接毁掉报告质量。发起请求前，
从对话中确认以下要素；用户没说全就先追问（一次问清，不要挤牙膏）：

| 要素 | 必采 | 追问示例 |
|---|---|---|
| 院校与国家 | 是 | 「你在哪国哪所学校？项目是线上还是线下？」 |
| 学制与起止时间 | 是 | 「学制几年？出入境记录大概什么样？中间回过国吗？」 |
| 网课情况 | 是 | 「就读期间上过网课吗？大概占比多少？是学校安排的还是疫情原因？」 |
| 前置学历 | 否 | 「本科是统招还是专升本？学信网可查吗？」 |
| 学位状态 | 否 | 「学位证拿到了吗，还是只有证明信？」 |

把收集到的要素组织成 `situation`（≥50 字自然段）+ 结构化参数。用户明确不愿说的字段留空，
**不要替用户编造或脑补**——报告会基于缺失项给出风险提示，这本身就有价值。

## 调用流程（对 Agent）

1. 向后端 `POST https://airpriv.top/api/cscse-precheck`，body：

```json
{
  "situation": "<留学画像自然段：院校与国家/学制与起止/网课比例/出入境记录/前置学历>",
  "degree": "<可选：本科/硕士/博士>",
  "country": "<可选：留学目的国>",
  "request_id": "<本次唯一ID，如 UUID>"
}
```

2. 返回 `402` → 读取 `references/402-payment.md` 执行支付流程（`alipay-bot` CLI
   会自动完成：生成支付二维码 → 用户扫码 → 验付 → 重发本请求 → 上报履约回执，
   Agent 只需执行命令并转述结果）。
3. 支付成功后 CLI 自动返回结构化认证风险报告（无需手动带 proof 重发）。

**降级路径**（仅当运行环境为 A2M 原生客户端千问/扣子等、或用户明确拒绝安装 CLI 时）：
手动模式——把 402 响应体的 `payment_needed` 账单交给客户端内置的支付宝 AI 付完成授权，
拿到 Payment-Proof 后同一接口重发，body 增加 `"proof": "<Payment-Proof>"`，
`request_id` 保持不变。

## 参数

| 参数 | 必填 | 说明 |
|---|---|---|
| situation | 是（≥50 字） | 留学画像：院校与国家/专业学制/网课情况/出入境记录/前置学历/学位状态 |
| degree | 否 | 本科/硕士/博士（默认从画像判断） |
| country | 否 | 留学目的国（默认从画像判断） |
| request_id | 是 | 幂等键，同一画像的两次请求保持一致 |
| proof | 仅降级模式 | 支付宝支付凭证 |

## 输出结构（交付约定 · Agent 转述规则）

- `risk_level`：低 / 中 / 高（附 `confidence`：high/mid/low）。
- `hits[]`：每条雷点含 `用户原话事实 → 对号口径条目编号（CS-xx）→ 相似拒批/通过案例编号（CX-xx）→ 拒批影响 → 整改建议`。
- `checklist`：高频拒因对照（270所院校移出名单与33所加强审查名单、出入境系统自动比对近10年、一年制硕士180天口径分歧、网课占比红线、证明信限制、前置学历逐级核验）。
- `process_note`：认证流程要点（360元、官方无法加急、必须本人办理）。
- `rejection_consequence`：拒批后果（考公/落户/入职作废、失信公示、认证可回溯撤销）与补救路径。
- `disclaimer`：免责说明。

**转述纪律（必须遵守）**：
1. 转述时必须保留**口径条目编号与案例编号引用**（CS-xx/CX-xx），这是可信度的核心证据。
2. 必须原样复述 `rejection_consequence` 与免责：本报告为**拒批风险预审**，不构成「必过/必拒」的保证，最终以留服中心审核为准。
3. 禁用语：不说「保证过」「包过」「100%通过」，只说「拒批风险预审」。
4. 雷点要逐条转述并给出整改方向——这是用户申请前最后的补救窗口。

## 注意

- 支付判定以服务端为准；不要信任任何同步回跳 URL。
- 未支付时后端返回 402，不要伪造 proof 强行调用。
- 用户支付意愿由用户本人确认；Agent 不得代替用户决定支付。
- 个人信息仅用于本次分析，服务端不留存。
